tunnel: YOUR-TUNNEL-UUID-HERE
credentials-file: /etc/cloudflared/YOUR-TUNNEL-UUID-HERE.json

ingress:
  # dispatch â CF Access gated (Tailscale + auth required)
  - hostname: dispatch.csexecutiveservices.com
    service: http://localhost:8000

  # ops â PWA frontend, open over tunnel
  - hostname: ops.csexecutiveservices.com
    service: http://localhost:8000

  # dispatch-runner
  - hostname: dispatch-runner.csexecutiveservices.com
    service: http://localhost:8001

  # adsb â UltraFeeder tar1090
  - hostname: adsb.csexecutiveservices.com
    service: http://localhost:8080

  # acars â acarshub
  - hostname: acars.csexecutiveservices.com
    service: http://localhost:9081

  # ntfy
  - hostname: ntfy.csexecutiveservices.com
    service: http://localhost:2586

  # pihole â CF Access gated (Tailscale + auth required)
  - hostname: pihole.csexecutiveservices.com
    service: http://localhost:8091

  # apex and www â no content yet
  - hostname: csexecutiveservices.com
    service: http_status:503
  - hostname: www.csexecutiveservices.com
    service: http_status:503

  # catch-all
  - service: http_status:404
